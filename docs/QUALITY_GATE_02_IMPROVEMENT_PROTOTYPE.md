# 品質ゲート2 — 改善プロトタイプ

検証対象: **病院STANDARD / 品質ゲート用経路**  
開始方法: タイトル → `廃病院からLIVE配信を開始`（盤面選択の推奨回線からも同じ経路へ接続可能）
基準時間: PLAYING開始から最終脱出まで180〜300秒  
現在判定: **HOLD / 入口・初回入力改修後の実ブラウザ再確認と人間オーナー承認待ち**

## 1. ベースライン監査

`main@11929e7` は自動チェックに合格していたが、次の理由で品質ゲート2を差し戻した。

- `chase` 分岐が到達不能で、短い逃走が存在しない
- 撮影を一度も使わずクリアできる
- 最初の怪異を見逃しても同接が増え、撮影との因果が崩れる
- MISSED / IGNOREDにも「記録済み」と誤案内する
- 通常脱出ではLIVE残留が発生せず、残留時も全UIが表示される
- 2盤面、周回、複数エンディングが混在し、3〜5分の検証経路が分離されていない

## 2. 依存関係とタスク状態

| ID | 担当 | 前提 | 成果物 | 状態 |
| --- | --- | --- | --- | --- |
| PT-GD-01 | ゲームデザイン | 企画固定 | 必須の撮影→同接→危険ループ | 完了 |
| PT-LD-01 | レベルデザイン | PT-GD-01 | 病院STANDARD固定検証経路 | 完了 |
| PT-ENG-01 | エンジニアリング | PT-GD-01 | 成功撮影のみ同接上昇 | 完了 |
| PT-ENG-02 | エンジニアリング | PT-LD-01 | 3撮影の進行ゲートと再出現 | 完了 |
| PT-ENG-03 | エンジニアリング | PT-ENG-02 | 観測者接近、CHASE状態、逃走成功条件 | 完了 |
| PT-ENG-04 | エンジニアリング | PT-ENG-03 | LIVE単独終端と所要時間判定 | 完了 |
| PT-QA-01 | QA | PT-ENG-01〜04 | 型検査、単体試験、ビルド | 完了 |
| PT-QA-02 | QA / 人間 | PT-QA-01 | PC実機3〜5分通し試遊 | 旧入口で完走済み / 入口改修後の再確認・人間確認待ち |
| PT-QA-03 | QA / 人間 | PT-QA-01 | ゲームパッド相当・スマホ横画面 | 旧入口でスマホ横確認済み / 改修後・reduced motion待ち |
| PT-OWN-01 | 人間オーナー | PT-QA-02〜03 | 品質ゲート2承認 | 前提待ち |

## 3. 必須体験

| # | 体験 | 実装上の合格根拠 | 判定 |
| --- | --- | --- | --- |
| 1 | 暗い廊下を歩く | 左右移動、歩行・走行、暗部マスク | 旧入口で合格 / 現入口は実ブラウザ再確認待ち |
| 2 | ライトで照らす | 独立照準、平滑追従、3段階光量 | 旧入口で合格 / 現入口は実ブラウザ再確認待ち |
| 3 | 光が遮られる | opaque / partial / slattedと光漏れ | 自動試験合格 |
| 4 | PIPだけに異変 | Main非表示、PIPはTier 1以降表示 | 自動試験合格 |
| 5 | コメントが先に反応 | TELEGRAPH中に決定論的バースト | 自動試験合格 |
| 6 | 撮影で同接増加 | RECORDED成功時だけTierを上げる | 自動・静的確認合格 |
| 7 | 同接増加で接近 | 520→240→80、TENSION下限18→32→48 | 自動試験合格 |
| 8 | 短い逃走 | 終盤PIP怪異からCHASE、停止で圧上昇、非常口で成功 | 旧入口で合格 / 現入口は実ブラウザ再確認待ち |
| 9 | LIVEだけが残る | 脱出後にMain/PIP/Chatを破棄し、LIVE単独画面を表示 | 旧入口で30秒残留確認 / 現入口は再確認待ち |

専用経路は通常の病院全編から分離した4区間・4怪異・収集物なしのセッションである。最初の3怪異の撮影を順番に要求し、最後の天井怪異でCHASEを開始する。観測者の追加撮影による旧エンディング分岐は無効で、`EXIT_USED`を通った場合だけ必須シーケンスが完成する。

## 4. 自動品質結果

- `npm run lint`: 合格
- `npm test`: 136 / 136 合格
- `npm run build`: 合格
- 新規純粋ロジック: 4区間の隔離経路、必須撮影ゲート、解決状態別の失敗文、180〜300秒判定、到達可能なCHASE分岐、必須マイルストーン順序、同接TENSION下限、怪異演出のTENSION反映

PLAYING中かつ非停止状態だけを有効時間として加算する。Journal、Chat、タブ非表示、GAMEOVER、回転ダイアログの停止時間は除外する。PLAYING開始、各必須撮影、CHASE開始・成功、脱出、終了の経過秒と順序判定は、最終LIVE要素の `data-prototype-*` 属性へ記録する。改善プロトタイプのLIVE画面は自動終了しない。

React統合、Canvas上の視認性、実所要時間、初見理解は単体試験では判定しない。

2026-07-15の改修前実ブラウザ通しでは `234203ms / IN_TARGET / sequence-complete=true` を実測した。ただし、追跡解除後の安全地点で130秒の観察を追加しており、熟練操作の自然な到達時間は約2分だった。入口・初回入力改修後の値ではないため、初見プレイとして無理なく180〜300秒になるかは再確認を残す。

## 5. 人間オーナー試遊チェック

1. タイトルの「廃病院からLIVE配信を開始」から1操作で探索へ入り、開発工程名が表示されないことを確認する
2. 最初の10秒で移動、ライト照準、撮影を理解できるか
3. ライト中心の床・扉・棚・怪異を即認識できるか
4. MainとPIPが同じ場所を映していると感じるか
5. コメントを見てPIPを自発的に確認するか
6. 未撮影時は同接237のままで、撮影後だけ2,370以上へ増えるか
7. 足跡、PIP限定人影、反復する車椅子の3撮影を通らない限り先へ進めず、再挑戦できるか
8. 同接上昇で観測者が近づき、終盤のCHASEへ自然につながるか
9. 逃走が短く明確で、理不尽な死亡にならないか
10. 脱出後にLIVE以外が消え、タイトル回収として伝わるか
11. 最終LIVE要素の `data-prototype-duration-band` が `IN_TARGET`（180〜300秒）か
12. PC、ゲームパッド相当入力、スマホ横画面でUIが重ならないか

全項目を満たした場合のみ、人間オーナーが `品質ゲート2：承認` を記録する。不合格時はVertical Sliceへ進まず、この経路だけを修正する。

## 6. Ultracode監査（2026-07-15）

最新 `main@e196e2e84743d3e25af3b14b6bf6efa1b29d7d74` を起点に、設計・実装・QAの3監査を並列実施した。詳細は [品質ゲート2 Ultracode監査報告](./QUALITY_GATE_02_ULTRACODE_AUDIT_2026-07-15.md) を参照する。

監査で修正した主な問題は、改善プロトタイプ最初の足跡がベッド遮蔽と失効距離の組み合わせで撮影不能だった進行停止、低FPS時の有効時間過少計測、フォーカス外のグローバル入力、タブ非表示復帰時のRAF時間飛び、PIP操作後のフォーカス喪失、スマホ横の目的UI重なりと再表示タイマー、現在バッテリーとPIP表示の不一致である。

最終判定は **HOLD**。S/A級の既知不具合は修正済みだが、自然な3〜5分尺、reduced-motion実機エミュレーション、スマホでの目的再表示→閉じる操作、実タブhidden→resumeの通し確認を人間ゲート前提として残す。このため自動マージ条件を満たさない。

## 7. 入口・初回入力の再監査（2026-07-15）

`main@d85cf84389f1942adbd4c6f2b993cc0666281709` から `codex/gate-2-immersive-entry-loop` を作成し、Creative Direction v4、Vertical Slice & Gimmick Direction v4、Art & Asset Bible v4を基準に、統括ディレクター・実装・QAの3監査を並列実施した。提示されていた `QUALITY GATE 2 / 改善プロトタイプ` 画面はプレイヤー体験ではなく開発ランチャーが前景化した状態であり、合格見本にはしない。

| 重要度 | 問題 | 原因 | 修正 | 現在の確認 |
| --- | --- | --- | --- | --- |
| A | PLAYING遷移後、入力前にも最初の怪異と計測が進む | Mainのフォーカス状態が停止条件に含まれない | programmatic focusと実入力を分離し、最初の移動・走行・しゃがみ・マウス照準・ライト・調査・撮影までMain/PIP/Chat・内部計測・表示時計を停止 | 純粋入力試験・ソース回帰試験合格 / 実ブラウザ15秒待機は再確認待ち |
| A | スマホの最初のタップが操作ではなく開始オーバーレイ解除だけに消費される | 操作ボタンより上に不透明なフォーカス層が存在 | 全面層を撤去し、最初の移動・ライト・調査・撮影で探索面を有効化 | ソース回帰試験合格 / 実タッチは再確認待ち |
| B | 工程名・時間判定・品質ゲート名が物語より先に見える | 開発用ゲートカードを全ユーザーへ常時表示 | タイトル主CTAを病院専用経路へ直結し、表示文言を劇中回線へ変更。内部識別子は非表示属性へ保持 | 文言回帰試験合格 |
| B | 667×375でプレビューがCTAより先になり、開始導線と三画面比較が沈む | モバイルDOM順、短辺横画面、compact Chatの定義不足 | コピーとCTAを先行し、667pxは2カラム、633px以下は1カラム化。PIP直下に最新Chatを常設し、目的表示幅をPIP外へ制限 | CSS・build合格 / 実画面再確認待ち |
| B | 入口で主怪異の完成形を先に見せる | タイトルと病院カードでobserver atlasを表示 | Tier 0のobserver atlasを撤去し、病棟・生活痕・三画面不一致を入口の主役に変更 | 静的確認合格 / 実画面再確認待ち |
| B | 入口プレビューと開始直後のChatが三画面校正に見えない | タイトルに独立PIPがなく、通常病院の人間コメントを初期投入 | 同じ廊下の遅延PIPを追加し、開始時はSYSTEM校正1件だけ、実入力後にイベントChatを解禁 | 文言・データ試験合格 / 実画面再確認待ち |

自動品質は `npm run lint`、`npm test`（136 / 136）、`npm run build`、`git diff --check` が合格した。入口配線、開発用語の非表示、初回実入力待機、SYSTEM校正Chatは新しい回帰試験で固定している。

実ブラウザ再確認は未完了である。ローカル開発サーバーはHTTP 200を確認したが、Codex内ブラウザは初回接続失敗後にセキュリティポリシーでナビゲーション操作が拒否され、別ブラウザ面への迂回は禁止されている。結果は捏造せず、1440×900、667×375タッチ、reduced motion、15秒停止整合、自然な3〜5分完走を **HOLD / 未確認** とする。これらを再実行し、S/A/Bが0になるまでマージしない。人間オーナーの正式承認も未記録である。

## 8. 現入口の実ブラウザ証拠計画

現入口の合格証拠は次の4ランを編集なしで採取する。共通して横スクロールがなく、Main・PIP・Chat・mission・CAPTURE・toast・操作群がviewport内に収まり、相互の重なりが0、タッチ対象が44×44px以上であることを矩形値とスクリーンショットで残す。

| Run | 条件 | 必須証拠 |
| --- | --- | --- |
| R1 | 1440×900 / 通常設定 | タイトルから1操作でPLAYING、15秒無入力で位置・電池・同接237・SYSTEM 1件・PIP静止画・表示時計0秒が不変。最初のDまたはマウス照準が同じ入力で反映され、Main/window focus喪失とhidden中は全時計が停止する |
| R2 | 844×390 / reduced motion | PIP下のcompact Chat、mission、toast、操作群が非重複。静止PIPのCAPTUREを最初の入力にして撮影判定・時計開始・Main focus復帰を確認し、reduced状態でもPIP限定手掛かりとChat位置情報が残る |
| R3 | 667×375 / touch | CTAが初期viewport内。右移動を最初の実touchとして保持し、解除操作だけに消費されず位置と時計が進む。全タッチ対象44×44px以上、横スクロールとUI重なりなし |
| R4 | 1440×900 / 新規セッション | 無停止・待ち時間水増しなしの自然な通しプレイ。3撮影→同接上昇→CHASE→EXIT→LIVE単独を180〜300秒で完了し、`data-prototype-duration-band="IN_TARGET"`、`data-prototype-sequence-complete="true"`、30秒後もLIVEだけが残る |

R4は停止試験と兼用しない。操作に必要な自然な観察だけで3〜5分になることを、人間オーナーが無編集録画と質問回答で判定する。チェックポイント待機で尺を合わせた完走は機能確認には使えるが、自然尺の合格証拠にはしない。

Art & Asset Bible v4について、現repoのruntime atlas 3点はmanifestの寸法・SHA-256と一致する。一方、concept sourceからruntime-readyへ昇格した制作履歴の人間アート検品は未記録であり、V4全アセットの正式承認済みとは扱わない。今回の凍結範囲では既存runtime atlasとコードネイティブUIのみを使用し、入口でTier 0の観測者完成像を先出ししない。
